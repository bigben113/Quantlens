[project]
name = "quantlens-ai-service"
version = "0.1.0"
description = "QuantLens AI service (FastAPI) - foundation health slice."
requires-python = ">=3.13"

[tool.pytest.ini_options]
pythonpath = ["."]
